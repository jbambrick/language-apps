import { DisplayVocabularyListNamePipe } from './display-vocabulary-list-name.pipe';

describe('DisplayVocabularyListNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayVocabularyListNamePipe();
    expect(pipe).toBeTruthy();
  });
});
