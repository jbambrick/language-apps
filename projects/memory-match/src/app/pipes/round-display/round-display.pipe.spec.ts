import { RoundDisplayPipe } from './round-display.pipe';

describe('RoundDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
